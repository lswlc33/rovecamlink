<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      miuix-blur declares minSdk 32 because the shader that does the blur is a
      `RuntimeShader` (API 33) and the fallback is a `RenderEffect` (API 31). The library
      is not otherwise tied to those versions — `isRuntimeShaderSupported()` and
      `isRenderEffectSupported()` are public precisely so callers can gate on them, and the
      only classes in the artifact that name an API-31/33 type are the two factory classes
      nothing loads until a blur is actually built. Overriding the floor lets the app keep
      shipping to 24 and render solid bars there instead of refusing to install.
    -->
    <uses-sdk tools:overrideLibrary="top.yukonga.miuix.kmp.blur" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- requestNetwork() with a specifier needs CHANGE_NETWORK_STATE; without it the
         system rejects the request with a SecurityException naming this permission. -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Camera permission for scanning the camera's WiFi QR code. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!--
      Bluetooth: find the camera and wake its hotspot *before* any Wi-Fi exists.
      neverForLocation keeps the scan out of the location story; ACCESS_FINE_LOCATION
      above is still required for Wi-Fi scan results, and below API 31 for BLE scan.
    -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <!-- required=false: a phone without BLE must still install and use Wi-Fi connect. -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="33" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".RoveCamApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="RoveCamLink"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.RoveCamLink">

        <!--
          Portrait-locked on purpose: the live view rotates the *picture* with the
          phone's own attitude (core.media.rememberDeviceOrientation), and letting the
          activity rotate too would turn the frame twice. The field report asked for
          exactly that split — 「不是旋转应用，只旋转画面」.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Temporary read grant for an exported diagnostics TXT (see core.log.AndroidLogStore). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
