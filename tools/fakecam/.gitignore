# Generated by generate.py. Reproducible, and its byte sizes have to match the
# numbers hard-coded in UiTestDevice.files() — committing it would let the two
# drift apart without anyone noticing.
DCIM/
